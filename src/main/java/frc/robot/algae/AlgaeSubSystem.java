package frc.robot.algae;

import frc.cmd.CmdScheduler;
import frc.cmd.SubSystem0;

/**
 * A {@link SubSystem0} that manages the Algae claw
 */
public class AlgaeSubSystem extends SubSystem0 {
    
    public AlgaeSubSystem(CmdScheduler scheduler) {
        super(scheduler);
    }
}
