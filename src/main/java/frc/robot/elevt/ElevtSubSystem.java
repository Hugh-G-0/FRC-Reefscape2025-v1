package frc.robot.elevt;

import frc.cmd.CmdScheduler;
import frc.cmd.SubSystem0;

/**
 * A {@link SubSystem0} that manages the elevator
 */
public class ElevtSubSystem extends SubSystem0 {
    
    public ElevtSubSystem(CmdScheduler scheduler) {
        super(scheduler);
    }
}