package frc.robot.climb;

import frc.cmd.CmdScheduler;
import frc.cmd.SubSystem0;

/**
 * A {@link SubSystem0} that manages the climber
 */
public class ClimbSubSystem extends SubSystem0 {
    
    public ClimbSubSystem(CmdScheduler scheduler) {
        super(scheduler);
    }
}
