package frc.robot.coral;

import frc.cmd.CmdScheduler;
import frc.cmd.SubSystem0;

/**
 * A {@link SubSystem0} that manages the Coral dropper
 */
public class CoralSubSystem extends SubSystem0 {
    
    public CoralSubSystem(CmdScheduler scheduler) {
        super(scheduler);
    }
}
